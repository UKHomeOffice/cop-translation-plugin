---
apiVersion: v1
kind: Secret
metadata:
  name: {{.NAME}}
type: Opaque
data:
  authClientSecret: {{.KEYCLOAK_SECRET}}
  sessionName: {{.SESSION_NAME}}
  sessionSecret: {{.SESSION_SECRET}}
  mobileidKey: "{{ file .SECRET_PRIVATE_SERVICE_INTERFACE_GATEWAY_MOBILEID_KEY | b64enc }}"