---
apiVersion: v1
kind: Service
metadata:
  labels:
    name: {{.TRANSLATION_NAME}}
  name: {{.TRANSLATION_NAME}}
spec:
  ports:
  - name: https
    port: 443
    targetPort: 10443
  selector:
    name: {{.TRANSLATION_NAME}}